// javascript

  
function areaOfCircle(r){
    area = 3.14 * r * r;
    
    return area;
}
let radius = 5;

area = areaOfCircle(radius);
console.log( area + " is Area of circle whose radious is " + radius )

area = areaOfCircle(2);
console.log (area + " is Area of circle whose radius is " + radius)

